@model List<Oppg1.Models.TestBestilling>

<h2>Bestilling</h2>

Her er dataene:
<table>

    <tr>
        <th>
            @Html.DisplayNameFor(model => model[0].dato)
        </th>
        <th>
            @Html.DisplayNameFor(model => model[0].avgang)
        </th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.dato)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.avgang)
    </td>
</tr>
    }
    



</table>



Må du endre bestillingen? Klikk tilbake til bestilling for å endre (:
<!--For å komme tilbake til bestillingsvindu-->
<div>
    @Html.ActionLink("Tilbake til bestilling", "Index")
</div>